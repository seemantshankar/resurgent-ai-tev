package com.resurgent.tev.parser.classify;

/**
 * Kind of shared nomenclature description across expanded formula dependencies.
 * Descriptive only — never a valid economic rollup or sum instruction.
 */
public final class SharedDependencyKind {
    public static final String LEAF = "leaf";
    public static final String ANCESTOR = "ancestor";

    private SharedDependencyKind() {}
}
