# Structural confidence never drops a Candidate

When discovery can justify both a coverage parent and a narrower Candidate, it keeps both. Structural confidence is an explanation on the Candidate. It is not a cutoff, merge rule, or delete switch. Omitting a plausible grouping would hide cells from later LLM classification, which owns `Scratch` / `Orphan`.
